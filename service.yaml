apiVersion: v1
kind: Service
metadata:
  name: cyber-threat-service
spec:
  selector:
    app: cyber-threat-dashboard
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  type: LoadBalancer
